# Local backend URL the dashboard talks to. Defaults to relative paths
# when unset (works when served from same origin as BossSignal backend).
# Set to a full URL when running the dashboard separately or behind a tunnel.
VITE_API_BASE=http://127.0.0.1:6700
